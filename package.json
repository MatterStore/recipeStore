{
  "name": "recipestore",
  "version": "1.0.0",
  "repository": "https://github.com/matterstore/recipestore.git",
  "author": "MatterStore",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "workspaces": {
    "packages": [
      "frontend",
      "backend"
    ]
  },
  "scripts": {
    "test": "echo \\\"Error: No test specified\\\" && exit 1",
    "start": "node backend/app.js",
    "backend": "node backend/app.js",
    "frontend": "cd frontend && npm start && cd ..",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  }
}
